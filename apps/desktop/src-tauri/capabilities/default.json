{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. App commands are exposed via the invoke handler; this grants the baseline core (event/window) APIs the UI needs.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:allow-open-url",
    "dialog:allow-open"
  ]
}
